name: "Podcast Gen"
author: "Guy"
description: "podcast gen test docker"
runs: 
  using: "docker"
  image: "Dockerfile"
branding: 
  icon: "git-branch"
  color: "blue"
inputs: 
  email:
    description: email address
    required: true
    default: ${{ github.actor }}@localhost
  name: 
     description: name
     required: true
     default: ${{ github.actor }}